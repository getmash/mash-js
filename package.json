{
  "name": "mash-js",
  "description": "Earn more for what you build, create and develop. Gain more customers because there’s no commitment barrier.",
  "author": "Mash Contributors",
  "keywords": [
    "lightning",
    "bitcoin",
    "creators",
    "mash",
    "monetize"
  ],
  "packageManager": "yarn@3.2.3",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "yarn workspaces foreach -p run clean",
    "build": "yarn clean && yarn workspaces foreach -ipt run build",
    "lint": "eslint --ignore-path .eslintignore --ext .ts,.tsx packages/*",
    "lint:fix": "eslint --ignore-path .eslintignore --ext .ts,.tsx --fix packages/*",
    "test": "jest --colors",
    "test:config": "jest --showConfig",
    "test:watch": "jest --watch --colors",
    "release:add": "yarn changeset add",
    "release:status": "yarn changeset status --since origin/main --output",
    "release:version": "yarn changeset version",
    "release:publish": "yarn changeset publish"
  },
  "devDependencies": {
    "@babel/preset-env": "^7.19.3",
    "@changesets/changelog-github": "^0.4.7",
    "@types/babel__preset-env": "^7",
    "@types/eslint": "^8",
    "@types/jest": "^29.1.1",
    "@typescript-eslint/eslint-plugin": "^5.39.0",
    "@typescript-eslint/parser": "^5.39.0",
    "babel-jest": "^29.1.2",
    "eslint": "^8.24.0",
    "eslint-config-prettier": "^8.5.0",
    "eslint-plugin-import": "^2.26.0",
    "eslint-plugin-jest": "^27.1.1",
    "eslint-plugin-prettier": "^4.2.1",
    "jest": "^29.1.2",
    "jest-environment-jsdom": "^29.1.2",
    "prettier": "^2.7.1",
    "ts-jest": "^29.0.3",
    "tslib": "^2.4.0",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@changesets/cli": "^2.25.0"
  }
}
